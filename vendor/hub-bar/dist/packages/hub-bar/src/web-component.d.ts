/**
 * Web Component wrapper for HubBar — for non-React host apps.
 *
 * Usage (after importing the script):
 *   <hub-bar
 *     current-product="rmm"
 *     api-base="https://api.theoneops.app"
 *     signalr-endpoint="https://api.theoneops.app/api/signalr"
 *   ></hub-bar>
 */
export {};
